name: Realse workflow
on:
    release:
        types: [published]
jobs:
    release:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repository
              uses: actions/checkout@v3

            - name: Print Release Info
              run: echo "A Release  has been published"